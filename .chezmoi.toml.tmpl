# .chezmoi.toml.tmpl
# Chezmoi bootstrap config — generated on `chezmoi init`, not managed as a target
# Template variables resolve before any other dotfile is applied
# Docs: https://www.chezmoi.io/user-guide/setup/

sourceDir = {{ .chezmoi.sourceDir | quote }}

[data]
    name              = {{ promptStringOnce . "name"              "Full name"                                           | quote }}
    email             = {{ promptStringOnce . "email"             "Email address"                                       | quote }}
    hostname          = {{ .chezmoi.hostname | quote }}
    os                = {{ .chezmoi.os       | quote }}
    wsl_distro        = {{ promptStringOnce . "wsl_distro"        "WSL distro name (e.g. Ubuntu-24.04)"                | quote }}
    win_projects_root = {{ promptStringOnce . "win_projects_root" "Windows projects path (e.g. C:\\Users\\pvnkmnk\\projects)" | quote }}

[git]
    # Set to true to auto-commit + auto-push on `chezmoi apply`
    autoCommit = false
    autoPush   = false

[diff]
    pager = "less -R"
